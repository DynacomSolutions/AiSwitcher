# Tasks

Track product work, bugs and acceptance evidence. Routine Git operations are not tasks.

| ID | Workstream | Status | Owner | Evidence and acceptance gaps |
| --- | --- | --- | --- | --- |
| 1 | Preserve Grok's explicit memory control through the identity launcher | Done | grok_memory_fix | Exact-key forwarding and four real-subprocess regressions are source-reviewed. Focused QA completed successfully; full CI on `97ae2de` passed typecheck and 609 tests across 76 files with zero failures ([run](https://github.com/DynacomSolutions/AiSwitcher/actions/runs/34216796298)). Compiled launcher inspected live: both launcher and real Grok child receive `GROK_MEMORY=0`. Installed binary matches the tested candidate byte-for-byte, with a rollback copy; other launchers and shared memory settings are unchanged. Other session markers and explicit child overrides remain intact. |
| 2 | Real `ais limits`/`ais usage` for AWS Bedrock identities (AWS Budgets + Cost Explorer) | Done | aws-spend | LIVE verified on this machine 2026-09-10 against both Bedrock identities: `ais limits acme-bedrock` renders `budget: acme-bedrock-monthly 0% resets Oct 1, 12:00 AM` under an "AWS Bedrock" section (budget limit $1000.0, actual $0.0, TimeUnit MONTHLY); the dev identity renders `budget: acme-bedrock-dev-monthly` ($300 limit, $0 actual). `ais usage --tool=codex` renders both as "AWS Bedrock" rows with EST. COST $0.00 from Cost Explorer UnblendedCost filtered to Amazon Bedrock (genuine zero in the trailing 3-month window, consistent with the budgets' $0 actual spend); regular codex identities still render as OpenAI rows. Typecheck green; 43 new fixture-driven tests (aws-profile, budgets mapper/overage/SSO classification, Cost Explorer report mapping) all pass; full `bun test` shows exactly the same 10 pre-existing environment-timing failures (codex-backfill, migrate-ais-home, tree-merge) as a clean HEAD checkout, verified by re-running those files on a stashed tree. Acceptance gap: no over-budget identity exists to photograph live overage rendering (unit-tested only); one transient DNS blip was observed once and is now retried (3s/8s) like every other fetcher. |
| 3 | Spend-cap enforcement: block launches and kill active wrapped sessions when an AWS account hits its Budgets cap | Done | spend-guard | LIVE verified read-only on this machine 2026-09-10 (SSO valid): one full cycle computed `acme-prod` account ...2281 budget acme-prod-bedrock-monthly cap $1000, local estimate $1931.66 month-to-date vs AWS-reported $0.00 (Cost Explorer + budget actual), effective $1931.66, BREACHED+enforced; `acme-dev` account ...6767 cap $300, local $72.30, healthy. Launch gate measured: fresh cache 0.2-0.3 ms decision (<50 ms budget), inline offline estimate ~0.4 s per codex identity (<1.5 s budget), and the gate BLOCKS `acme-bedrock` with the full refusal while the dev identity passes in 1.6 ms. Unit/e2e: 68 new tests green (breach transitions, max-blend, staleness/degraded rules, cache round-trip, codex+claude log readers, temp-HOME refusal run of the real wrapper asserting exit 1 + exact message, canary child with the marker env SIGTERMed by the REAL /proc scanner while an unmarked twin survives, never-kill set, cache persistence). `ais doctor` renders the live `aws-spend-guard` section (in budget / BREACHED / DEGRADED per account); `ais limits` budget windows carry the over-cap note; WebUI dashboard gained the spend-guard card (build green). Full `bun test`: same 10 pre-existing environmental failures as main, all in codex-backfill/migrate-ais-home/tree-merge. Acceptance gap: no live kill of a real breached session was performed (the canary test covers the killer end-to-end with a synthetic identity; killing the user's real sessions was out of bounds); the daemon scheduler's interval tick loop is thin-tested (start/stop wiring, not long-run behaviour); `acme-prod` having two COST budgets means the guard enforces on the largest-limit one while `ais limits` renders both windows. |
| 4 | Parallel `ais upgrade` with a stable status-list UI | Done | upgrade-ui | All 8 tool upgrades run in parallel behind a pure transition reducer (`src/cli/upgrade-status.ts`); zai/ali dedup preserved structurally via `planUpgrades` (follower awaits the leader's single physical install). Real TTY run via `script` (all tools current, so rows show "already X"; grok native update ran): final frame `claude ✔ already 2.1.267 (0.6s)` / `ali - shares installer with zai` / summary `done, installed/upgraded 7 real CLIs in 3.7s.`, exit 0, no npm noise in the frame. Real piped run printed start/finish lines + same summary, exit 0. `bun run typecheck` green; 47 upgrade tests green; full suite 613 pass with 10 pre-existing environment-dependent failures (migrate/sync/backfill timeouts) reproduced identically on a clean `main` checkout. Gap: no in-frame demonstration of a version transition `a -> b` (every tool was already up to date at run time); cancellation-path exit codes are unit-tested but were not reproduced against a live TTY Ctrl-C. |
| 5 | ali-cookie-refresh | Done | ali-cookie | Root cause (all evidence-backed live 2026-09-10): Alibaba expired the chrome-auth-personal pod browser's console login server-side (last harvest success 2026-09-05T10:30Z; no `login_aliyunid_ticket` cookie, console renders "Log In to Use"); the pod tab sat at `about:blank` after pod churn so the refresh's starting-URL precondition bailed every 10-minute tick with no self-heal and no alarm; and when the tab WAS on an Alibaba page the harvest wrote logged-out cookies (39 captured live, `login_aliyunid_csrf` present WITHOUT login) over console-cookie.txt reporting success. Fix: refresh navigates to the console itself, verifies the login-ticket cookie before writing, throws `AliAuthRefreshError` with a remediation hint; failures now exit 1 from `ais auth refresh`, log loudly from the scheduler (escalated past `ESCALATION_THRESHOLD` = 3 consecutive), surface in `/api/auth/refresh` status (`consecutiveFailures`), `ais doctor` (new ali probe, `degraded` status) and `ais limits --tool=ali` ("(last refresh attempt <ts>: <error>)"). Live verification: refresh against the real personal identity throws the precise not-signed-in error with hint; console-cookie.txt mtime unchanged by the failed refresh (poisoning stopped); scheduler recorded consecutiveFailures=1 in ~/.ais/web/auth-refresh-state.json; `ais limits --tool=ali` and `ais doctor --tool=ali` render the enriched degraded messages. Typecheck green; bun test 624 pass with 10 pre-existing failures on main (identical on base: migrate/codex-backfill/tree-merge lock-probe timeouts on this host). Remaining MANUAL step (cannot be automated, Alibaba requires interactive MFA): `ais auth login personal --tool=ali`, complete sign-in over the printed noVNC URL, then `ais auth refresh --tool=ali --identity=personal`; the 10-minute keep-alive holds the session afterwards. Note: the running console daemons still deploy the old code until their image is rebuilt. |
| 6 | WebUI identity auth: managed login flows, per-identity status badges | Done | web-auth | Managed logins for claude (script PTY + paste-code stdin path) and codex/grok/kimi (device-code, plain pipes); pi/opencode degrade to terminal handoff (documented; opencode prompts verified non-injectable live); zai/ali remain key/cookie writes. LIVE verified against a temp-home daemon on port 4799: `GET /api/auth` statuses, claude flow surfaced the real OAuth URL and reached `waiting`, codex flow surfaced URL + device code, both cancelled cleanly, pi returned the terminal handoff. NO OAuth was completed (no code pasted, no account state touched); daemon and temp dirs removed after. claude login writes `.credentials.json` on success is UNVERIFIED (requires completing a real login). `bun run typecheck` green; `bun test` 634 pass with only the 10 pre-existing environmental failures (same set fails on clean main: 12); `apps/web` pnpm build green. |
| 7 | webui-files-path-expansion | Done | files-fix | Files tab ENOENT `<home>/.ais/~/.ais` fixed: `safeResolve` now expands a leading `~`/`~/` (via exported `expandTilde`) against the real home before resolve, with both containment checks intact after expansion; `~name` stays literal. Second bug confirmed and fixed: WebUI `getFileContent`/`saveFile` never sent `root`, so every editor open/save hit 400 `"root" is required`; client now passes root on GET/PUT and the query key includes it. Tests: `expandTilde` unit cases, post-expansion 403 escapes (including the old `<root>/~/..` collapse), literal `~name` file, and a subprocess fixture booted with a fake HOME asserting the exact reported flow (`~/.ais` root selection, nested tree, read/write round trip). Typecheck green; web `tsc && vite build` green; `bun test` matches clean main (10 pre-existing lock-timeout failures in migrate/merge tests verified on stashed main); live HTTP smoke on 127.0.0.1 passed all 8 steps including 403 escape and 400 missing-root. |
| 8 | tui-status-cost-limits | Done | tui-status | Status tab now renders a per-identity, per-provider "Identity cost and limits" panel merged from the existing `/api/limits` + `/api/usage` endpoints (no server changes): EST $ (tokscale token estimate) kept distinct from REAL $ (provider-reported `extraCost`/`overage` spend), worst limit window gauge, scheduled reset plus blocking note and `manualReset.availableCount` credits, per-source error cells. Live-verified 2026-09-10 against the real console daemon (port 47129) inside tmux: real identities (workco, personal, workco-team, hospico, retailco) rendered with live-moving percentages, reset times, `$0.00` vs `-` real-spend distinction, unavailable/429 error cells, and graceful recovery after a cold-cache scan; `j/k` scroll and the Limits tab regression-checked. Fixed en route: scan endpoints now use per-endpoint client timeouts (50s limits / 70s usage) that outlast the server's 45s/60s scan budget, which previously aborted every cold-cache poll. Gates: `cargo fmt --check` clean, `cargo clippy --all-targets` zero warnings, 8 unit tests pass (summary merge), `cargo build --release` succeeds. Gaps: fixture unit tests cover the merge logic only, not pixel layout; session/today/month cost buckets are not available from the existing API (all-history estimate shown instead). |
| 9 | tui-arrow-tab-navigation | Done | tui-nav | `aistui` tabs now cycle with Left/Right arrows (wrap at both edges), alongside the existing Tab / Shift+Tab and 1-7 bindings; Up/Down and j/k keep scrolling the active page. Six `cargo test` unit tests cover the cycle and regressions (6 passed); `cargo clippy --all-targets -- -D warnings` and `cargo fmt --check` are clean; `cargo build --release` succeeds. Smoke-run against the live console daemon in tmux: Right/Left switched and wrapped tabs, Help page shows the new key line, footer shows the arrow hint, q quit cleanly. Gap: full CI covers the TypeScript side only; no Rust gate exists in ci.yml, so the Rust checks above ran locally. |
| 10 | herdr-metadata-bridge | Done | herdr-bridge | Daemon-side bridge (`src/server/herdr-bridge.ts`, wired like spend-guard in serve.ts/app.ts, `AIS_HERDR_BRIDGE=0` opt-out, GET `/api/herdr-bridge`, WebUI dashboard card) pushes per-pane `$ais_identity`/`$ais_session`/`$ais_week`/`$ais_month`/`$ais_limits` tokens to herdr's sidebar via `herdr pane report-metadata --source ais`; zero herdr source changes and zero herdr processes touched (CLI calls only, plus killing our own timed-out CLI client spawn). LIVE verified read-only 2026-09-10 against the real herdr 0.8.2 and a private daemon instance on 127.0.0.1:4799 (managed:false, no state file, auth-refresh and spend-guard disabled, machine-local config `push:false`, config + harness deleted afterwards): one full cycle enumerated the real panes, attributed 17 of them (codex identities personal/workco/workco-team/acme-bedrock, opencode workco, claude personal; bare shell panes correctly skipped), pulled limits through the /api/limits scan pathway, and produced correct per-pane tokens (e.g. workco `s:43% w:100%`, personal `s:3% w:100%`, workco-team `w:60%` with session honestly omitted, Bedrock identity tokenless); state `active`, lastError null. KEY FINDING vs the task brief: this machine's herdr 0.8.2 build already ships `pane report-metadata`, so the bridge lands `active` here; feature detection is behavioural (`herdr pane report-metadata --help` probe, never version parsing) so older builds classify `pending` ("report-metadata needs herdr >= 0.9.0") and the probe re-runs every cycle until an upgrade flips them active with no config change. Token contract, state machine and activation story documented in AGENTS.md and docs/API.md. `bun run typecheck` green; 34 new tests green (fixture pane-list/process-info captured live, probe classifier, token computation incl. missing windows/categories, config loader, idle/pending/active transitions, downgrade sticky guard, push:=false); full `bun test` 879 pass with exactly the 10 pre-existing environmental failures of clean main; apps/web `tsc && vite build` green. Acceptance gap: the real metadata WRITE was never fired at the user's running herdr (read-only verification constraint; push path covered by injected-dependency tests only), and herdr's sidebar rendering of `$ais_*` tokens is unverifiable from this repo. |
| 11 | bedrock-usage-local-vs-real | Done | bedrock-display | User-reported display regression fixed: PR #42's Cost-Explorer-only fetch stuffed REAL dollars into EST. COST with zero token columns, hiding all local tracking for AWS Bedrock. Bedrock rows now render like every provider (messages/input/output/cache-read + EST. COST from the shared session-log readers over the same month-to-date window the spend guard estimates; `shared/local-spend.ts` moved from spend/ and extended, guard byte-identical, its tests untouched), while the REAL AWS figures (Cost Explorer month-to-date + trailing-window total + enforced budget limit/actual) ride in a new `UsageResult.realCost` rendered as a dimmed `└ real AWS month-to-date:` sub-row under each Bedrock row plus one trailing real total line; CE failure degrades into `realCost.error` ("unavailable", never a fabricated zero) without blanking local tracking. Sub-row chosen over an extra column: the figure exists for one provider, so a near-empty column would widen every table. LIVE verified 2026-09-10 (`bun run src/ais.ts usage --tool=codex`): `AWS Bedrock | acme-bedrock | 7,028 | 624,334 | 2,833,612 | 661,521,472 | $2,356.48` with `└ real AWS month-to-date: $0.00 (budget actual $0.00 of $1,000.00, reported lag)`; dev identity `$72.30` under budget $300. RECONCILED with the spend guard: `ais doctor` live minutes later reports local $2356.90 vs usage's $2356.48 for the same identity (drift = live codex activity between runs; guard cache 06:42Z had $2354.02), Cost Explorer $0.00 + budget actual $0.00 in both pathways. JSON payload carries report (local) + realCost + dailyUsage for WebUI chart; WebUI mirrors sub-lines per Bedrock row + real total (pnpm build green); TUI REAL $ maps realCost -> RealSpend ($0.00/$1,000.00), only answered queries map (cargo fmt/clippy --all-targets clean, 16 tests pass, cargo build green). Gates: `bun run typecheck` green; `bun test` 859 pass with the same 10 pre-existing environmental lock-timeout failures as clean main (codex-backfill/migrate-ais-home/tree-merge). `ais limits` budget windows untouched (out of scope). Gap: WebUI sub-line and TUI REAL $ verified by build + unit tests and payload inspection, not photographed against a redeployed daemon (running daemons deliberately untouched). Follow-up 2026-09-10 (branch `fix/real-cost-column-aligned-subrow`): the freeform full-width sub-row/real-total lines (user: "breaks the table design") rebuilt as genuine column-aligned table rows in the same 8-column grid: label `└ real AWS month-to-date` dimmed under IDENTITY, centred dashes in the four count columns, EST. COST empty (semantic rule intact), real dollars + compact budget context (`real $0.00 · budget $0.00/$1,000.00 (reported lag)`) right-aligned in EXTRA COST which auto-widens; errored queries render `real unavailable: <reason>` (truncated to 60 chars) in the same cell; the trailing real total is now a `└ real AWS total` row with the same shape. Rendering change only, data collection untouched. WebUI sub-lines aligned the same way (label under Identity, dashes across numeric columns, value right-aligned in the Notes cell, Est. cost cell empty). LIVE verified 2026-09-10 (`bun run src/ais.ts usage --tool=codex`): both Bedrock sub-rows and the real-total row sit inside the borders with continuous separators; gates: `bun run typecheck` green, `bun test` 910 pass with the same 10 pre-existing environmental lock-timeout failures, `pnpm build` green in apps/web. |
| 12 | usage-cost-breakdown | Done | usage-breakdown | Per-identity WHAT-used-the-tokens view: `ais usage breakdown [identity] [--tool=] [--days=30] [--json]` (collector `src/cli/usage/breakdown.ts`), `/api/usage/breakdown` in the isolated scan worker (60s cache, 240s child ceiling), web `/breakdown` view and tui Breakdown tab (,/. cycles identity pair). One documented attribution rule: prompt-side tokens (input/cache read/cache write) always on the "conversation" row, output split evenly across the turn's tool calls (claude tool_use blocks; codex per-turn function_call items), estCostUsd at CHAT_MODEL_PRICES list rates (models.dev, checked 2026-09-10), estimate-only labelling everywhere. LIVE on this machine 2026-09-10: claude/workco 30d top rows conversation $628.89 (5,756 msgs), Bash 2,649 calls $130.64, Write $5.51, Edit $2.50; codex/workco 30d conversation $5,363.00 (128,196 responses), exec 77,242 calls $589.86, send_message $42.52; `ais usage` EST. COST all-time for comparison: codex/workco $10,013.09, claude/workco $10,346.14 (30-day subset + models.dev vs tokscale rates explain the gap; same order of magnitude, plausible). Endpoint verified end to end (scan child + envelope): HTTP 200 in 54s for codex/workco 30d. Gates: `bun run typecheck` green; `bun test` 862 pass with exactly the 10 pre-existing environmental lock-timeout failures; 17 new collector tests green against in-memory fixtures (attribution rule, MCP rollup + per-tool detail, torn lines, mtime + timestamp window filters, unavailable paths); apps/web `pnpm build` green; apps/tui clippy clean, 15 tests pass, build green. Gaps: attribution is heuristic by design (the logs record usage per model turn, never per call; the "chat" conversation row dominates prompt-side totals and output splits are even, not exact); grok/kimi/zai/ali/pi/opencode degrade to `unavailable` (no per-call data in their local stores, verified); stores of 10GB+ can exceed the 240s child ceiling (explicit 504; smaller --days scans less); TUI/web show the estimate labels but the TUI truncates at 12 rows by design (full list via web/CLI --json). |
| 13 | sanitize-client-identifiers | Done | sanitize | Public-repo hygiene sweep: every private identity, AWS SSO session/profile, budget name and machine path in the tree replaced with neutral stand-ins via one consistent mapping (workco, workco-team, acme, acme-bedrock, acme-bedrock-dev, acme-dev, acme-prod, acme-sso, Acme-SSO, acme-bedrock-monthly, acme-bedrock-dev-monthly, acme-prod-bedrock-monthly, retailco, hospico, studioco); applied to docs/API.md examples, TASKS.md evidence rows (dollar figures, account-id fragments such as ...2281 and percentages kept verbatim so the evidence stays truthful), test/aws-profile.test.ts, the spend suite, spend-guard and herdr-bridge tests (assertion strings renamed with the fixtures), the TUI summary tests, and the captured herdr pane fixtures (local paths, hostname and home-directory usernames neutralised). Verification: case-insensitive identifier-family grep (`dyna[c]om|n[a]zare|p[c]g-|phoe[n]ix-court|psyc[h]ozone|m[a]ngobay` over the whole tree) returns only deliberate keeps (src/installer.ts REPO constant, k8s/*.yaml devdeploy hostnames, repo URLs in TASKS.md row 1, a base64 integrity-hash coincidence in apps/web/pnpm-lock.yaml) plus LICENSE/Dockerfile copyright attribution flagged for the owner. Gates: `bun run typecheck` green; `bun test` green apart from the same 10 pre-existing environmental lock-timeout failures as main. Gap: git history untouched by design (tree-only sweep); k8s hostnames and the installer REPO constant remain by deliberate exception. |
| 14 | spend-guard-warn-mode | Done | spend-warn | Breach response made configurable via `mode` in ~/.ais/config/spend-guard.json (`spend/config.ts`): `"warn"` is the DEFAULT (absent key and absent file alike, by owner decision) and preserves the cap math while refusing nothing and killing nobody; `"enforce"` keeps the original gate refusal (exit 1) and transition kill (SIGTERM/grace/SIGKILL) byte-for-byte. Gate: warn breach prints one loud stderr warning (account, budget, cap, effective spend, "warning only - not blocking; set mode=enforce in ~/.ais/config/spend-guard.json to block") and the launch continues; config now loaded by the gate/doctor/limits annotate (one tiny JSON read). Killer: warn mode still computes and records breach transitions but killAccountSessions is unreachable (no scan, no signal) and the skip is logged; status DTO exposes config.mode with recentKills empty. Surfacing: doctor renders BREACHED (warning) vs BREACHED (enforced), `ais limits` notes say "warning only, not blocking" vs "launches blocked", WebUI card shows an amber warning badge (and OVER CAP (warning only) header) in warn mode. Tests: temp-HOME gate e2e covers BOTH modes (absent-config warn: exit 0 + warning + child ran; enforce: exit 1 + refusal + child never ran), warn canary survives a breach tick untouched while the enforce canary is still SIGTERMed, config loader absent-file/absent-key/typo->warn cases. LIVE on this machine 2026-09-11 (live spend-guard.json ABSENT -> default-absent path): one real cycle with a private cache in /tmp (live cache and daemon untouched, private state deleted after) resolved mode=warn with defaults intervalS=300/killGraceS=10; prod account BREACHED (effective $4046.59 vs $1000 cap, Cost Explorer clean), DTO recentKills [], killer log "entered breach: mode=warn ... NOT killing", scan and signal invoked zero times. Gates: `bun run typecheck` green; `bun test` 921 pass with exactly the 10 pre-existing environmental lock-timeout failures (migrate/tree-merge/codex-backfill); apps/web `pnpm build` green. |
| 15 | console-state-isolation | Done | state-isolation | Incident (owner decision: "it should auto update that file but should NEVER take shit down"): the host console daemon and the k8s pod daemon both run `ais web --serve-internal` and both read/write the same ~/.ais/web/server.json (pod hostPath-mounts ~/.ais and runs hostPID), so one side's lifecycle churn killed the other's daemon (observed live: pod Deployment restarts, Reason "Completed", exit 0, after host-side stop/start churn) and the two clobbered each other's port/token. Live-confirmed at fix time: the host state file named the POD's pid 1656755 (kubepods cgroup) while the real host listener was 1652680, and the pod showed 3 restarts. Fix: `AIS_WEB_STATE_DIR` env override in consoleWebDir() (src/server/state.ts) relocating every console write (server.json, auth-refresh-state.json, file-edit backups, src/server/files.ts now derives file-backups from consoleWebDir() instead of aisHome()); k8s/deployment.yaml sets AIS_WEB_STATE_DIR=/web/state with a pod-local emptyDir mount (Dockerfile already created /web/state); stop/restart never signal blind: /proc/<pid>/cmdline must carry an ais basename somewhere in argv AND the --serve-internal marker before SIGTERM, otherwise a loud refusal ("refusing to kill pid N - it is not a console daemon (cmdline: ...)") and the state file is left untouched (/proc-less macOS falls back to the legacy kill); start with a dead/foreign state pid plus an occupied port now fails fast with "port N already in use" instead of a vague 10s timeout. LIVE on this machine 2026-09-11: controlled host stop/start back-to-back killed ONLY the host daemon (port 47129 freed then rebound, new pid 2265699) while the pod daemon stayed alive the whole time; a second instance with AIS_WEB_STATE_DIR=/tmp/... on port 4799 wrote its own server.json under the override while the host's server.json sha256 stayed byte-identical, and stopping the 4799 instance left the host healthy; the refusal path: state pid hand-poisoned to a sleep 999 child -> `ais web stop` exited 1 with the refusal message, the sleep child survived, the real daemon kept serving; the dead-pid start path errored "port 47129 already in use" in 0.28s without claiming started. All test processes/dirs cleaned up; healthy host daemon left running on 47129. Gates: `bun run typecheck` green; `bun test` 934 pass with exactly the 10 pre-existing environmental lock-timeout failures as main; 13 new tests (consoleWebDir override/default/blank, writeServerState mkdir+write under the override, cmdline matcher incl. setsid/dev-mode/marker-less refusals, stop refusal leaving state file, real SIGTERM on a matching fake-cmdline pid, /proc-unreadable fallback). apps/web and the TUI untouched. Gap: the cmdline guard cannot distinguish two REAL console daemons sharing one state file (both match the rule by design); that residual window is exactly what the state-dir override removes once the pod redeploys with the new image/env (manifest change takes effect on the next image build + devdeploy, not retroactively). |
| 16 | `ais herdr` tmux wrapper with responsive overview panel | Done | herdr-wrapper | `ais herdr` (src/cli/herdr.ts) create-or-attaches the "ais-herdr" tmux session: real herdr client LEFT, `aistui --overview` RIGHT (new apps/tui/src/overview.rs, width-responsive compact panel: per-identity estimated cost, session/week(/month) bars, next reset, spend-guard breach marker, and highlights for identities whose herdr panes are open on the left via the bridge DTO's new `focused`/`agent` fields, the focused pane's identity strongest). herdr never bundled: shared resolveHerdrBinary (AIS_HERDR_BIN > PATH > ~/.local/bin), absent herdr is a clear "ais upgrade" error; `ais upgrade` gained a herdr row only when herdr is installed. LIVE 2026-09-11 on an isolated tmux socket (`--tmux-socket`, no user session touched): left pane captured showing the real herdr UI; right pane 18 live identities with costs, bars, resets, a breach marker and honest "no window data" rows; resized 42 -> 30 -> 24 -> 18 capturing the Full/Compact/Bars breakpoints (three bar groups drop month at 30; the header drops the clock before gluing); highlight colours proven with `capture-pane -e`: cyan for open, YELLOW for the focused pane's identity (a private 4799 daemon running this branch's bridge reported the single focused pane; the production daemon was never restarted and its old DTO degrades highlights to open-only, by design). Remote: `--remote` left pane against an unreachable host keeps herdr's own error visible via remain-on-exit; `--remote-ais` degrades honestly (wrapper note line, "no highlight source", local data; the ssh -L tunnel lives inside the panel pane and is torn down on any failure, so no fabricated remote view). `ais upgrade` live: herdr row appeared, ran the captured `herdr update`, surfaced herdr's own in-session refusal as a failed row, then from a herdr-free environment finished `herdr: done already 0.8.2` with the 8-CLI summary and exit 0. Gates: `bun run typecheck` green; `bun test` 974 pass with exactly the 10 pre-existing environmental failures; `cargo fmt`/`clippy --all-targets` clean, 32 Rust tests, release build. Gaps: focused-marker evidence used a private daemon (the production daemon predates the `focused` DTO field and picks it up on its next rebuild); a remote SUCCESS could not be honestly exercised (no reachable remote ais host; the degradation paths were verified instead); the attach path is unit-tested but was not driven live (no interactive TTY in the test environment). |
| 17 | pi-identities-extension | Done | pi-identities | Pi (v0.85.1, `@earendil-works/pi-coding-agent`, the earendil/badlogic pi-mono agent) gained an in-session AIS identity surface: a self-heal-installed TypeScript extension (`src/pi-extension/ais-identity-extension.ts`, installed by the `ais pi` wrapper into `$PI_CODING_AGENT_DIR/extensions/ais-identity.ts`, version-stamped so stale copies refresh, embedded in the compiled binary via Bun's `type: "text"` import attribute) providing a persistent footer status (`ais <identity>: <provider>/<model>` via `ctx.ui.setStatus`), `/ais` (compact widget table of every provider's credential type + catalogue model count with honest gap notes), `/ais use <provider>[/<model>]` (real in-session switching through pi's own `pi.setModel()`, no request sent, defaults for new sessions untouched), and `/ais identities` (registry listing + launch commands). Plus `ais auth sync --tool=pi <identity>`: ADD-ONLY credential sync (planner `planPiCredentialSync`/`syncPiCredentials` in pi-auth.ts) - missing providers added from the six AIS registries (source resolution: explicit flag > same-named identity > single-identity registry > honest skip note; alibaba-plan reuses the Crush-catalogue translation; existing entries NEVER overwritten because the freshest rotating OAuth copy may already live in auth.json, per the kimi-store one-credential law). Investigation evidence (from the installed package, not assumptions): extensions are jiti-loaded TS needing no compilation (docs/extensions.md); pi re-reads auth.json per request via a `dev:ino:size:mtimeNs:ctimeNs` file-revision check (dist/core/auth-storage.js + dist/utils/paths.js `getFileRevision`) - which makes in-session whole-identity swapping technically possible and therefore an explicit HONESTY DECISION: it is deliberately NOT implemented because a swapped-in rotating OAuth copy would refresh into the hosting identity's auth.json and mis-attribute the account (the exact kimi-rotation failure mode kimi-store.ts prevents), so `/ais identities` says switching requires a relaunch. LIVE verified 2026-09-11 against real pi 0.85.1 driven through the real wrapper (`bun src/pi.ts --identity=...`) in tmux with a sanitized throwaway COPY of an identity configDir under a temp HOME (live configDirs untouched, everything torn down after): extension self-heal installed on launch and pi's own startup listed `ais-identity.ts` under `[Extensions]`; footer `ais identity-a: anthropic/claude-opus-4-8`; `/ais` table with per-provider credential types, model counts (anthropic 14, opencode-go 27, ...) and gap notes (`local-kimi: no credential in auth.json`); `/ais use zai/glm-5.3` flipped pi's OWN footer to `(zai) glm-5.3 - high` and the AIS status line to `ais identity-a: zai/glm-5.3` (widget refreshed current-first via `model_select`), notify carrying the honest "new sessions still start on the identity's configured default"; `/ais use nope` -> `Error: no models for provider "nope" in Pi's catalogue`; `/ais identities` rendered both fixture identities with launch commands and the relaunch note; zero billable requests (pi cost footer stayed $0.000). Sync verified in the same sandbox against fixture registries: added alibaba-plan + merged the models.json catalogue while preserving local-kimi, second run idempotent ("Nothing to add"), bedrock/opencode-go honest gap notes, no secrets printed. Gates: `bun run typecheck` green; `bun test` 1011+ pass with only the pre-existing environmental lock-timeout failures of clean main (codex-backfill/migrate-ais-home/tree-merge; 8-10 depending on machine load, all in that family); 45 new tests (extension helpers + command behaviour incl. injected-fixture harness that never reads live state, installer fresh/stale/current/unwritable/env-target, sync planner + writer add-only semantics + idempotency, source-picker rules, jiti-transpile check of the exact shipped source). `apps/web` and Rust untouched. Gaps: no live multi-identity switch demo (the relaunch-only boundary is the documented design, not a limitation to test away); the hasConfiguredAuth may-still-fail note on /ais use is defensive only (pi's setModel already refuses unauthenticated providers, so it never fired live); privacy-check fails identically on clean main on this machine (pre-existing environmental, unrelated); pi's 10-line widget cap truncates the provider table tail for identities holding more than ~7 providers (current provider sorted first by design). |
| 18 | upgrade-skip-limits-render | Done | upgrade-limits-fix | Two user-reported regressions, both root-caused and live-verified. (1) `ais upgrade` run inside a herdr pane: herdr's updater legitimately refuses in-session, but the runner counted a FAILED row and exited 1. Fix: an `insideHerdr` pre-check (`insideHerdrPane`, the HERDR_*-env detector shared with the `ais herdr` nesting guard) runs before any spawn and settles the row as a dedupe-style skip, `herdr - skipped (herdr is running; detach and rerun ais upgrade, or run herdr update yourself)`, counted in `skipped`, exit 0; defensive net classifies herdr's own captured refusal text ("run `herdr update` outside herdr ...") as `HerdrAttachedError` -> the same skip, never onFailure, while real failures (network) still fail. LIVE on this machine, both paths, real CLI under a pty: with HERDR_* set the final frame shows the skip row, zero herdr spawns, summary `done, installed/upgraded 7 real CLIs in 3.4s. (1 skipped)`, exit 0; with AIS_HERDR_BIN pointed at a shim exiting 1 with the real refusal text, same skip row and exit 0. (2) `ais limits` streamed an endlessly growing log instead of updating in place. Root cause (byte-level `script` captures of the real command, not code reading alone): the report grew to 89 physical lines as rows resolved, far taller than any terminal, and withLiveRender's cursor-up clamps at the screen's top row once the frame exceeds terminal height, so every 80ms redraw scrolled the whole report again -> a stream. NOT a PR #40 regression: live.ts/limits dispatch/report were untouched after #40 (per-file history checked); the report simply outgrew the technique. Fix: withLiveRender now clamps every frame to rows-1 lines (fallback 24 when the pty reports no window size) with a dim "+N more lines" marker, so ONE frame stays on screen no matter how tall the report gets, and the limits live path erases the frame on settle (new `eraseOnFinish` option) and prints the full uncapped report exactly once; en route fixed the live path double-appending spend-guard notes (annotate ran at store time AND every frame). BEFORE: 98 frames, each the full report, growing 49->89 lines, cursor-up up to 87 (clamped = scrolling). AFTER (30-row pty): 61 in-place frames of constant 29 lines (56 distinct: spinners animate in place), cursor-up never above 29, then exactly one 89-line full report, exit 0; a zero-size pty clamps at 23 lines. New byte-level regression tests (test/cli/live-stream.test.ts) patch process.stdout.write and assert the control-sequence stream: cursor-up+clear prefix on every redraw, constant clamped frame height, the +N marker, the fallback, eraseOnFinish, cursor restore. Gates: `bun run typecheck` green; `bun run build` compiles; `bun test` green apart from exactly the pre-existing environmental lock-timeout family of clean main (codex-backfill/migrate-ais-home/tree-merge, variance by host load as on main); `privacy:check --staged --local` clean. Gaps: in-place rendering verified via pty captures up to 30 rows rather than a resized interactive terminal; `--watch` inherits the same clamp but a long watch cycle was not driven live; the upgrade skip was not exercised against the REAL herdr binary in a real herdr pane (the user's exact scenario) because herdr processes are out of bounds for this session - covered instead by the env pre-check + refusal-shim live runs and unit tests. |
| 19 | oauth-credential-fork | Done | oauth-fork | Root cause (code + live evidence 2026-09-12): `ais auth import --tool=pi` (src/identities/pi-auth.ts) snapshots each native tool's OAuth grant into the pi identity's auth.json, but only kimi had the one-credential law wired (kimi-store.ts read/write-through); anthropic/openai-codex/xai had NO coordination, so the native CLIs and pi refreshed one grant independently. Live divergence map at fix time (fingerprints + drift only, no token values): 8 of 9 paired accounts held DIFFERENT refresh tokens across stores (claude/dynacom anthropic native b431ec0a vs pi d8249917, 23.5d staler; claude/personal 4b5d7498 vs 975a3005, 15.7d; all four codex pairs 10-20d; grok/dynacom 19.4d; kimi/dynacom 23.9h after a native-CLI-side refresh), only kimi/personal in sync thanks to AIS's own write-through; claude/phoenix-court-group's native store is logged out (empty tokens, untouched). The user's forced re-login is the stale-copy replay failure mode this law removes. Fix: the law generalised in src/identities/oauth-reconcile.ts (freshest-wins by JWT iat for codex/xai, numeric expiry for anthropic/kimi, native tie-break; write-through into each store's own shape, 0600 atomic temp+rename, one .ais-bak per rewritten native file; fingerprints sha256-8hex, never tokens), wired at AIS's boundaries: every `ais pi` launch self-heals (reconcilePiConfigDirOnLaunch, silent when in sync, never blocks launch), `ais auth sync --tool=pi` reconciles after the add-only pass, `ais auth import --tool=pi` reconciles BEFORE overwriting so a fresher pi copy survives; `ais doctor` gained a pi probe flagging diverged copies as degraded/forked with remediation. Live reconcile run once in write mode (judgement call per plan: all pairs had freshest=native, so every heal projected the native grant into pi's auth.json; native stores untouched except pi/personal's kimi-coding where PI had refreshed mid-run and the reconcile correctly pushed pi's grant into the native kimi store): 9 forks healed, dry-run re-verify all converged, mode 0600 + backups verified. Full write-through shipped for anthropic, openai-codex, xai AND kimi-coding (no provider needed detect-only degradation). Residual race documented in AGENTS.md (pi refreshes between reconciles are invisible until the next heal; window now one pi session at most, versus weeks before). Gates: bun run typecheck green; bun test 1043 pass with exactly the 10 pre-existing environmental lock-timeout failures of clean main (codex-backfill/migrate-ais-home/tree-merge); privacy:check --staged --local clean; 21 new tests (divergence detection, freshest-wins both directions, per-store shape mapping incl. grok multi-account + codex last_refresh + claude refreshTokenExpiresAt preservation, idempotence, no-op in-sync, dry-run, degenerate shapes, launch helper, doctor probe, render secrecy). Gaps: the reconcile cannot see pi-internal refreshes between runs (documented residual); the doctor probe reads stores at query time only (no watch mode); no multi-account grok store exists on this machine to exercise sibling-entry matching live (unit-tested with a fixture). |
| 20 | upgrade-actually-updates | Done | upgrade-verify | Incident (user, 2026-09-12): `ais upgrade` showed green rows (`claude ✔ already 2.1.268`, `grok ✔ updated (1.4s)`) while the real tools announced updates at launch. Root cause, reproduced live: (1) grok's native updater `grok update` exits 0 printing "v1.0.30 installed successfully" while npm's install-script policy blocks `@xai-official/grok`'s postinstall, so `~/.grok/bin/grok` stays 1.0.25 against xAI's stable channel 1.0.30 (verified by running the real updater: exit 0, `grok --version` unchanged) and `installOrUpgradeGrok` trusted that exit code with zero verification, its xAI-installer fallback only reachable on non-zero exit; (2) the npm "already up to date" short-circuit compared the pinned registry version to the managed package manifest and checked only the managed binary's EXIT CODE, never the version it reports, and nothing ever verified the binary on the RESOLVED path (what the shim execs); (3) the `@latest` fallback install ran npm without `--prefer-online`, so cached packument metadata could pin yesterday's release. Fix (src/cli/upgrade.ts): `extractSemver`/`compareSemver` parse every vendor `--version` shape (`2.1.268 (Claude Code)`, `codex-cli 0.154.0`, `crush version v0.93.1`, `grok 1.0.25 (sha) [stable]`); grok consults xAI's own stable channel (`https://x.ai/cli/stable` + GCS mirror, the same URLs its install.sh reads) as the authoritative latest, and an exit 0 whose post-version is still below the channel falls through to xAI's installer (which writes `~/.grok/bin/grok` directly, no npm postinstall to block), failing the row honestly ("grok still reports X ... channel latest is Y") if the version is STILL short; with the channel unreachable the row degrades to before/after evidence ("already X" for unchanged readable versions, herdr's precedent); npm installs now ALWAYS send `--prefer-online`, the short-circuit additionally requires the managed binary's reported semver to match the pin (older -> reinstall), and every successful install is gated post-install: manifest must equal the pinned version and the binary on the RESOLVED path must report at least it, else an honest FAILED row naming the shadowing install; `UpgradeInstallResult.reason` threads precise failure headlines through the parallel status rows. Live verification on this machine 2026-09-12, real installs: `claude: done 2.1.268 -> 2.1.269 (3.6s)` (post `claude --version` = 2.1.269, manifest 2.1.269), `Grok's native updater exited 0 but grok still reports 1.0.25 (xAI's channel latest is 1.0.30); reinstalling with xAI's installer` then `grok: done 1.0.25 -> 1.0.30 (7.7s)` (post `grok --version` = 1.0.30, `~/.grok/bin/grok` re-pointed at the new download, AIS shims byte-identical BuildID), `zai: done 0.93.1 -> 0.94.1`; idempotent second run exit 0 with `already X` rows (claude 2.1.269, grok already 1.0.30) and the herdr row skipped untouched via the documented HERDR_* env path. Gates: `bunx tsc --noEmit` green; `bun test` 1057 pass with exactly the 10 pre-existing environmental lock-timeout failures of clean main (re-verified on a stashed clean tree: identical 9+1 set in migrate-ais-home/tree-merge/codex-backfill); `bun run build` compiles; 15 new tests (semver helpers, channel resolver mirrors/malformed/offline, lying-exit-0 installer fallback + honest still-short failure, real-transition without installer, unreachable-channel already-X, resolved-shadow failure, manifest-did-not-move failure, reported-older reinstall, matching-parse already-X) plus the two retargeted freshness assertions. Gaps: the PATH-shadowing FAILED row is unit-tested only (no shadowed real install exists on this machine to break on purpose); the grok channel fetch is not ETag-cached (two light GETs per grok row, acceptable); claude's native channel was confirmed NOT ahead of npm (dist-tags stable 2.1.236 < latest 2.1.269, its updater uses @latest for npm installs), so no second version source was wired for npm tools. |
| 21 | oauth-proactive-refresh | Done | oauth-refresh | Closes the two gaps row 19 left open: nothing reconciled before a limits/usage READ, and nothing ever REFRESHED an expiring grant proactively (access tokens expired by design until the real CLI next ran - the weekly re-login tax). Fix: (1) RECONCILE-ON-READ - every OAuth-backed fetcher reconciles ONCE per fetch before reading (codex/claude via new `reconcileNativeProviderStores` in identities/oauth-reconcile.ts, the native-side counterpart of #60's pi sweep; kimi already read freshest + wrote through natively; grok's fetcher is a log scrape with no token read, honestly excluded), and codex retries once on the 401/token_expired family; (2) PROACTIVE REFRESHERS - identities/oauth-refresh.ts exchanges refresh tokens at each provider's endpoint (codex `https://auth.openai.com/oauth/token` + client id from the installed binary's own strings; anthropic `https://platform.claude.com/v1/oauth/token` + client id likewise; xai by plain OIDC discovery off the account entry's `oidc_issuer`/`oidc_client_id`, matching grok's own embedded docs; kimi reuses kimi-limits.ts's machinery) and writes the rotated grant through to every store per the one-credential law (atomic 0600 + backup); wired into the AuthRefreshScheduler registry (codex/claude/grok/kimi alongside ali) with cadence: inside AIS_AUTH_REFRESH_EXPIRY_HOURS (default 24) of access-token expiry OR at least once daily; manual `ais auth refresh <identity> --tool=<t>` (CLI, new for codex/claude/grok/kimi) and POST /api/auth/refresh force past the cadence; a provider `invalid_grant`/400/401 pins a revoked diagnosis to the grant's FINGERPRINT in the scheduler state (never a loop; a re-login mints a different token and refreshes resume), surfaced via status DTO (`revoked`, `lastDetail`) and doctor; codex doctor now renders "token expired (refreshable)" vs "revoked - re-login required" distinctly. LIVE verified 2026-09-12 on the incident identity (codex/personal, fingerprints only): access fp 2fef8c58 expired 16.9h ago in BOTH stores (native+pi in sync post-#60), `ais auth refresh personal --tool=codex` hit the honest revoked path - provider answered 400/401 to the standard grant - exit 1 with "re-login required: run `codex login` under this identity and re-import to pi"; `ais limits personal --tool=codex` reproduces the incident 401 (`"code":"token_expired"`) now carrying the actionable hint; stores byte-untouched by every failed attempt (fingerprint re-verified). ONE re-login of codex/personal is needed (grant killed by the pre-#60 fork damage; `codex login status` still reports file-level login, so only the provider can say no); after re-login the daemon refresher keeps it alive inside the 24h window. Gates: `bun run typecheck` green; `bun test` 1091 pass with exactly the 10 pre-existing environmental lock-timeout failures of clean main (verified on a stashed tree); `privacy:check --staged --local` clean; 49 new tests (per-provider request shape + rotation write-through in each store's shape incl. grok sibling preservation + codex last_refresh, non-rotation keeps old refresh token, revoked classification + fingerprint pin + skip + re-login resume, cadence decisions, native-vs-pi reconcile both directions, doctor classifier, scheduler DTO). Gaps: no live SUCCESSFUL refresh could be demonstrated (the incident grant is genuinely revoked; the one-alive sibling codex identity was deliberately NOT refreshed - the live-verify budget is one real refresh), so the success path is proven by injected-fetch tests only until the daemon deploy + first natural tick; the doctor's "refreshable" classification reads stores + scheduler state only and cannot know a CLI-side revocation until the daemon pins it (documented); daemon must be rebuilt for the scheduler to carry the new refreshers. |
| 22 | usage-parallel-freeze | Done | usage-freeze | Incident (owner, 2026-09-12): `ais usage` freezes and its per-target fetchers stop overlapping. ROOT CAUSE (PR #50, commit 1dca491): `fetchAwsBedrockUsage` was wired to `readIdentityLocalSpend`, a fully SYNCHRONOUS fs walk + whole-file readFileSync over every Bedrock-backed identity's month-to-date JSONL; inside the runBatched(6) fan-out each such read blocked the single event loop solid, so the live-render timer, every other target's completion callback and all concurrent fetchers stalled for the duration. Measured on this machine: each Bedrock identity's month-to-date tree is ~1.1GB (151 + 120 files, ~25k + ~17k records); the sync read of one tree takes 3.7-6.0s; in the real `ais usage --tool=codex` run a 20ms event-loop probe jumped from a 45ms worst gap to a 6037ms gap across exactly the `localSpend read: 6001ms (SYNC)` window, with zero target completions before/after it; a 1ms probe fired ZERO times during 51s of back-to-back sync reads (fully blocked loop). NOT the cause, ruled out with evidence: PR #59's live render (setInterval-only, no per-frame awaits), PR #60's OAuth reconcile (pi-launch/doctor/auth boundaries only, never the usage fetch path), #51's breakdown collectors (separate `usage breakdown` subcommand), tokscale's bunxQueue (cached-binary probe passes, spawns bypass bunx and run in parallel). FIX: shared/local-spend.ts gained `readIdentityLocalSpendAsync`, an async twin reading each file as bounded 256KiB decoded pieces via Bun's file stream (streaming TextDecoder bridges multi-byte chunk boundaries; EOF remainder flushed so torn final lines behave identically), walked with fs/promises, and a REAL macrotask `Bun.sleep(0)` between pieces: awaiting the Bun stream alone proved insufficient (a 1ms timer fired 0 times during a 3.2s read), so the explicit yield is load-bearing. Line scanners and the record reduction are shared verbatim with the sync path, so both produce byte-identical output by construction; the sync twin stays for the spend guard (launch gate, one identity, off the render's hot path). `fetchAwsBedrockUsage` now awaits the async reader (injected-reader dep widened to accept sync-shaped test fakes). AFTER (same machine, same command, unbuffered writeSync instrumentation, host load 120-435): event-loop worst gap 113ms across the whole 96.5s `--tool=codex` run (vs 6037ms freeze before), all 9 targets completed, and the two Bedrock reads (7.6s + 8.0s of local-log work) overlapped each other and the concurrently running tokscale children, both finishing by +14.7s while other children ran on to +96s; unscoped (25 targets) completes with fan-out 51.0s. Micro: async twin 3.6s vs sync 3.9s on the same ~1.1GB tree (no wall regression), 330/332 timer ticks, 21/18ms max gap, byte-identical `JSON.stringify` output. New regression tests: sync/async equivalence on a real on-disk tree and on unaligned string chunks, 1ms-timer-keeps-ticking during a slow chunked read, three concurrent reads wall ~= max not sum (~96ms vs ~240ms serialised), default reader streams a 6MB file in many chunks, and a fetcher-level two-concurrent-`fetchAwsBedrockUsage` overlap test. Gates: `bun run typecheck` green; `bun test` 1048 pass with exactly the 10 pre-existing environmental lock-timeout failures of clean main (verified twice); `bun run build` compiles. Observations filed for follow-up: (1) wall clock for codex-scoped/unscoped runs on this host is dominated by pre-existing tokscale full-history children scanning 10GB+ identities (unchanged by this fix; cold-cache runs exceed 15 minutes under load ~400-1200); (2) one 3.5s event-loop gap remains in unscoped runs from the PRE-existing zai/alibaba Crush reader (56MB crush.db on the known-slow /home/user/Projects mount, one correlated-subquery `.all()` in native sqlite, predates #50); (3) during one heavily-loaded run the loop froze ~200s in the tokscale-child phase, which also delayed the 120s spawn-kill timers; not reproducible on warm reruns (max gap 113ms), suspected Bun-under-contention pathology, worth its own investigation. |
