{
  "name": "ai-profile-switcher",
  "type": "module",
  "version": "0.2.1",
  "scripts": {
    "test": "bun test",
    "build": "bun run scripts/build.ts",
    "backup": "bun run scripts/backup.ts",
    "migrate": "bun run scripts/migrate.ts",
    "install:shims": "bun run scripts/install.ts"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14"
  },
  "dependencies": {
    "@clack/core": "^1.4.2",
    "@clack/prompts": "^1.0.0"
  }
}
